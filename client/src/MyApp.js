import React, {StrictMode} from 'react';
import {UseStatePlayground} from './hooks/useState';

export const MyApp = () => {
    return (
        <StrictMode>
            <UseStatePlayground />
        </StrictMode>
    );
};
